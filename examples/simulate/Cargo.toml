[workspace]

[package]
name = "simulate"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
plumbob = { path = "../..", features = ["std"] }
hdmi-hal = { version = "0.2.0" }
display-types = { version = "0.3.1" }
